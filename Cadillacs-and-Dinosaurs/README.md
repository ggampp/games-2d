# Sauria Riot

Jogo original neste repositório: pasta [`sauria-riot/`](sauria-riot/).

Beat 'em up 2D no navegador (Phaser 3). Arte, nomes e áudio próprios — não é o ROM da Capcom.

```bash
cd sauria-riot
npm install
npm run dev
```

`http://127.0.0.1:3000`
